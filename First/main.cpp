#include <iostream>
#include <list>
#include <stack>
#include <vector>
using namespace std;
int main()
{
	list<int> l(1);
	stack<int> s;
	vector<int> v(1);
	cout << v.size() << '\n';
	cout << s.size() << '\n';
	cout << l.size() << '\n';
	l.
}